# Supabase Workspace

This directory is reserved for local Supabase configuration and migrations.

Use `../docs/local-supabase.md` as the setup and migration runbook. Do not add seed/demo data once persistence is wired; migrations should define durable wallet-first tables, RLS policies, and negative authorization tests.
